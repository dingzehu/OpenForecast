# InfluxDB connection — copy this file to .env in the repo root and fill in your values
# .env is excluded from git (see .gitignore)

INFLUXDB_HOST=your-influxdb-host
INFLUXDB_USER=your-username
INFLUXDB_PASSWORD=your-password
INFLUXDB_DB=your-database-name
